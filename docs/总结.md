# 游戏王罕贵度 · 工艺着色器 —— 总结

> 一页读完这个项目做了什么、43 条罕贵度各是什么观感、哪些做不出来、怎么验的。
> 想深入看实现细节 → [`rarity-shaders.md`](rarity-shaders.md)；想跑起来 → [`../README.md`](../README.md)。

- **卡图**：`images/Shooting Quasar Dragon.jpg` —— Shooting Quasar Dragon（龙族/同调/效果 · 光 · ★12 · ATK 4000 / DEF 4000）
  —— **`images/` 下有几张就认几张**，面板里可以换
- **来源文档**：`游戏王罕贵度总结（OCG  TCG）.md`（**一个字都没改**），43 条罕贵度全部覆盖
- **做法**：17 个自己写的工艺着色器，一条罕贵度 = 一串图层叠在同一个卡面上
- **界面**：中文 / English / 日本語 三语，各 301 个键
- **跑法**：双击 `index.html`（`file://` 可用，无需构建）

---

## 1. 一句话讲清楚原理

**罕贵度的差别几乎全在"哪一块被加工了"上。**

面闪只有卡图有闪膜、银字只有卡名的笔画烫银、收藏闪是边框+卡名+卡图外框有彩虹。
所以先**从图里量出几块区域**，再让每个着色器选一块来加工：

| 区域 | 怎么定出来的 | 占比 |
| --- | --- | --- |
| **卡名笔画** | 卡名带里**按暗度抠**（不猜字形位置，换字体也跟得住），再膨胀 2px | 2.30% |
| **卡图** | 插画本身（亮度剖面量到的：x 96–718、y 224–836） | 44.2% |
| **效果框** | 含 ATK/DEF 带（y 878–1135） | 21.0% |
| **卡框** | 整卡 − 卡图窗外框 − 效果框（着色器按矩形现算） | 29.7% |
| （卡图外环） | 卡图窗外框 − 卡图内容 | 5.1% |

渲染就是：**背景 → 投影 → 原始卡面 → 一层层叠工艺**，每层采样同一张"没加工过的原始印刷"，
自己决定输出什么颜色、留多少 alpha。

---

## 2. 43 条罕贵度速查

**看这一列**：一眼能从画面上认出它的特征 → 就是配方在做的事。

### §1 基础罕贵度（12 条）

| 代码 | 俗称 | 一眼特征 | 图层 |
| --- | --- | --- | --- |
| `N` | 平卡 | 什么都没有 —— 基准 | （无） |
| `R` | 银字 | **只有卡名笔画变银**，卡图不闪 | `name` |
| `SR` | 面闪 | **只有卡图**有全息闪光，卡名还是黑的 | `holo`→`gloss` |
| `UR` | 金闪 | 卡图闪 **+ 卡名金色** | `holo`→`gloss`→`name` |
| `SER` | 银碎 | **斜向碎冰**膜，卡名和卡图都盖 | `diagonal`→`glitter` |
| `UTR` | 3D / 浮雕 | 插画被"压"出立体感，侧光下看得见凹凸 | `emboss`×3 |
| `HR` | 全息 / 鬼闪 | **整张卡变银白**，怪兽像幽灵浮出来 | `ghost` |
| `CR` | 收藏闪 / 彩虹闪 | 卡框、卡名、卡图外框有**跟着鼠标转的彩虹** | `rainbow`×2 |
| `PSER` | 白碎 | **整卡铺满**交叉闪膜，卡名是**会跳的白色碎闪** | `prismatic`→`name`→`glitter` |
| `20th SER` | 红碎 | **红色卡名** + 效果框上密排 "20th" 水印 | `diagonal`→`name`→`stamp`→`glitter` |
| `Starlight` | 星光 / 星闪 | 整卡极细平行闪膜，转卡时**整片一起扫过去** | `parallel`→`holo`→`glitter` |
| `QCScR` | 25周年碎 | 棱彩碎膜 + "25th" 水印 | `prismatic`→`stamp`→`glitter` |

### §2 高级与特殊罕贵度（10 条）

| 代码 | 俗称 | 一眼特征 | 图层 |
| --- | --- | --- | --- |
| `ESR` | 斜碎 / 额外碎 | 和银碎同为斜碎，但**斜得更陡、碎格更密** | `diagonal`→`glitter` |
| `GUR` | 黄金闪 | **整张卡变金**，但插画图案还认得出 | `metal`×2→`name` |
| `GScR` | 黄金碎 | 金箔底 + 碎冰膜 | `metal`×2→`diagonal`→`name` |
| `PlR` | 铂金闪 | 和金闪同一套，底色换成**更冷更亮的铂金** | `metal`×2→`name` |
| `PlScR` | 铂金碎 | 铂金 + 碎冰 | `metal`×2→`diagonal`→`name` |
| `GGR` | 鬼金闪 | 鬼闪的银白底 + 金框 | `ghost`→`metal`→`name` |
| `PUR` | 棱彩浮雕 | 浮雕打底 + 棱彩膜 | `emboss`→`prismatic`→`name` |
| `PCR` | 棱彩收藏闪 | 彩虹反射 + 整卡棱彩膜 | `rainbow`×2→`prismatic` |
| `Pharaoh's` | 法老闪 | **埃及文字图案**膜 + 淡金工艺 | `millennium`→`metal`→`name` |
| `NR` | 平罕 / 隐普 | **和 N 一模一样** —— 差别在封入率，不在画面 | （无，刻意如此） |

### §3 平行 / 爆闪类（10 条）

| 代码 | 俗称 | 一眼特征 | 图层 |
| --- | --- | --- | --- |
| `NPR` | 平爆 / 普钻 | 平卡底 + 整卡爆闪 | `parallel`→`holo` |
| `RPR` | 银爆 | 银字底 + 爆闪 | `name`→`parallel` |
| `SPR` | 面爆 | 面闪底 + 爆闪 | `holo`→`parallel` |
| `UPR` | 金爆 | 金闪底 + 爆闪 | `holo`→`name`→`parallel` |
| `PR` | 爆闪 | 纯粹一面**极细平行线光栅** | `parallel` |
| `KC Rare` | KC闪 | 线光栅 + 隔行错开的 **KC 标志** | `kc`→`gloss` |
| `Millennium` | 千年闪 | **埃及象形字**按格随机拼成的文字带；字自带彩色折射，竖线只在字之间 | `millennium` |
| `Starfoil` | 星箔 | 一格一颗**四角星** | `starfoil` |
| `Mosaic` | 马赛克闪 | **方格**闪膜，一格一个色相 | `mosaic` |
| `Shatterfoil` | 碎箔闪 | **不规则碎片**，碎片边发亮 | `voronoi` |

### §4 Duel Terminal（6 条）

`DT-N` `DT-R` `DT-SR` `DT-UR` `DT-SER` `DT-PR` —— 工艺与 OCG 同名罕贵度一致，
在 `js/rarities.js` 里用 `like:` 指过去（**图层只有一份定义**，不是抄的）。

### §5 其他活动 / 地区 / TCG 特殊（5 条）

| 代码 | 俗称 | 处理方式 |
| --- | --- | --- |
| `HL` | 联赛卡 | 文档说"特殊颜色卡名或闪膜"→ **绿色卡名** + 爆闪 |
| `DLR` | 联赛闪 | 文档说"卡名颜色多样"→ **蓝色卡名** + 爆闪；面板里能换任意颜色 |
| `CP / TP 等` | 比赛闪 | 文档自己说"版本差异大"→ 只给一个**中间态示意**，明确标注 |
| 韩文版 | — | 不是新工艺，是**发行版本** → 借面闪配方当代表 |
| 亚洲英文版 | — | 同上 → 借金闪配方当代表 |

---

## 3. 17 个工艺

| 类别 | 工艺 | 机制一句话 |
| --- | --- | --- |
| **闪膜** | `holo` | 宽光带全息，相位场由"视角 + 卡面亮度 + 噪声"驱动 |
| | `parallel` | 极细平行线光栅，**颜色只沿垂直线方向缓慢变** —— 这是爆闪的观感关键 |
| | `diagonal` | 斜光栅 + 沿栅格随机的碎片色相，碎片边发白 |
| | `prismatic` | 正反两个方向的光栅交叉，**交叉点炸白** = 白碎 |
| | `starfoil` | 一格一颗四角星（astroid 曲线） |
| | `mosaic` | 方格，格线压暗 |
| | `voronoi` | Voronoi 碎片，碎片边发亮 |
| **图案** | `kc` | 线光栅 + 图集里的 KC 标志 |
| | `millennium` | 4 个象形字按格随机拼（**风格化近似**）；字与字之间一道竖线，字面和竖线各是一层箔、都随视角变色。格子按像素等比（旧的 `squareGrid` 除以长宽比会把图案压扁） |
| | `stamp` | 20th / 25th 水印，斜排密铺 |
| **工艺** | `emboss` | **把卡面明暗当高度场**求梯度得法线，再用方向光打亮 |
| | `ghost` | 去色 + 提对比 + 冷偏移 → 银白幽灵 |
| | `metal` | 明暗**映射**到金属色 + 拉丝高光（不是加增益，否则整卡炸白糊掉） |
| | `rainbow` | 以高光点为圆心按极坐标取色相，鼠标一动整圈转 |
| | `name` | 卡名笔画烫金 / 烫银 / 换色 |
| **加光** | `glitter` / `gloss` | 细碎亮点 / 卡面扫光（这两个走**加法混合**，只输出"多加了多少光"） |

外加 4 个基础件（`base` `shadow` `bg` `maskdebug`）= **21 个片段着色器**，
以及一个 **3D 倾斜顶点着色器** —— 鼠标在卡上移动时卡片真的绕 X/Y 轴转，
同样这两个角度驱动闪膜的衍射相位（拿实卡看闪膜本来就是捏着卡慢慢转）。
**默认鼠标指到哪、那一侧往里沉**（像手指按住卡片），面板里可反向。

---

## 4. 换卡图 & 多语言

这两块都是"加东西不用改代码"的设计，细节见 [`rarity-shaders.md`](rarity-shaders.md) §10 / §11。

### 换卡图：`images/` 下有几张就认几张

```powershell
# 把图片丢进 images/，然后
node tools/embed-card.mjs      # 重新烘焙所有卡图与掩膜
```

刷新页面，参数面板最上面那个「卡图」下拉框里就有了。**卡片外沿是自动识别的**
（`tools/lib/detect.mjs`，烘焙与验证共用同一份实现）。

识别的三步都是实测换出来的：阈值写死 0.85 → 换一张"卡片只占画面 47% 宽"的图就失效；
改成自适应阈值 → 浅色卡贴浅色背景时上沿对比度不够、比例算错；
最后加"用已知的 59:86 比例校正"才稳。四个合成用例的四边误差是 `[0,0,1,1]` / `[0,11,1,0]` / `[0,0,1,1]`。

**换卡要注意**：卡框/卡图窗/效果框那几个矩形按**游戏王标准版式**写死、所有卡共用
（灵摆卡、连接怪、无效果怪对不上，得在 `tools/embed-card.mjs` 的 `CARDS` 表里按文件名覆盖）；
卡名抠图还写死了"深字浅底"的极性（金字压深红底的真正金闪得反过来）。

### 多语言：`Languages/` 下一个语言一个文件

```js
global.CardI18nPack['zh-CN'] = { code: 'zh-CN', name: '中文', texts: { 键: 文案 } };
```

| 语言 | 键数 |
| --- | --- |
| `zh-CN` 中文 | 301 |
| `en-US` English | 301 |
| `ja-JP` 日本語 | 301 |

301 键 = 界面骨架 38 + 43 条罕贵度 × 4 项 + 39 个参数 × 2 项 + 22 个预设 + 每张卡 3 项。
加一种语言 = 复制一个包改名 + 在 `languages.js` 里加一行，右上角下拉框即时切换。

**语言包是 `.js` 不是 `.json`**：`fetch('./Languages/xx.json')` 在 `file://` 下会被 CORS 拦掉，
而"双击 index.html 就能跑"是这个项目的硬要求。机制（localStorage 记住选择、缺键回退、
`{var}` 插值）与参考的那份文档一致，只有加载方式不同。

三层兜底：**当前语言包 → zh-CN 包 → 代码里的中文原文**。`js/ui.js` 里所有文案都用
`I18N.tOr(键, 原文)`，所以一个语言包都没加载成功也不会满屏裸键。

---

## 5. 哪些做不出来（没有藏起来）

| 文档里的说法 | 为什么做不出来 | 怎么处理 |
| --- | --- | --- |
| **NR 平罕**："外观与平卡相同，但封入率异常低" | 这条罕贵度的**唯一特征是封入率**，没有画面差别 | 渲染成和 N 完全相同，**一个像素都不改** |
| **UTR**："触摸有凹凸" | 触摸是物理属性 | 只做视觉浮雕，文字里明说触摸做不出来 |
| **CP / TP 等**："不同赛事、地区版本差异大" | 文档自己就说没有统一工艺 | 只给示意配方，明确标注 |
| **韩文版 / 亚英** | 不是新工艺，是发行版本 | 借配方当代表，说明里写清 |
| **卡面右下角的编号字母** | 图上没有，而且改它属于伪造卡面 | 只在页面说明条上显示代码 |
| **价格** | 受发行量/强度/品相/语言影响 | 完全不做 |

另外几条是**取值上的取舍**：`Starlight`/`PSER`/`QCScR` 文档都只写"整张卡覆盖闪膜"、
没写形状，我按观感做成三种不同的膜好让它们**看得出区别**；千年闪的"埃及文字"是
**风格化近似**不是考据；闪膜的流动动画是我加的（实卡不动，但屏幕上需要一点流动感）。

---

## 6. 验证

```powershell
node tools/verify.mjs      # 60 项：归档 / 掩膜 / 罕贵度表 / 编译 / 确定性 / 布局 / 倾斜 /
                           #        检测器合成用例 / 多卡图 / 多语言 / hash
node tools/effects.mjs     # 43 个罕贵度 + 17 个工艺 + 39 个参数 + 22 个预设实测
node tools/doc-check.mjs   # 核对两份文档里可机器判定的说法（52 项）
```

三套全 PASS。**逐参数扫描**会把每个参数改一遍、看画面动没动；**逐工艺扫描**会把
每个工艺单独留在卡上、和"平卡 N"逐像素比 —— 这两条是抓 bug 的主力。

### 工具真的抓出来的东西

前 8 条是扫描类工具抓的，后 4 条是"动手造第二张卡 / 把像素摊开看"抓的：

1. **部分覆盖的层会把整张卡重画一遍** —— `name` 写成 `if (mask<0.004) return tex` 看着无害，
   其实返回的 `tex` 带 alpha=1，等于在上一层成果上把原始印刷贴回来。
   表现是"黄金闪怎么调都不变金"，而且**完全不报错**。
2. **打开参数面板卡片会变小** —— 画布只监听了 window 的 resize，stage 变窄时没重新分配
   绘制缓冲区，被 CSS 缩着显示。现在用 `ResizeObserver` 盯 stage。
3. **倾斜方向两个轴是相反的** —— 鼠标往右是"右边朝你翘"，鼠标往下却是"下边往里沉"。
   单看一个方向都说得通，斜着晃鼠标就发现两边在打架。现在统一由一个参数控制。
4. **"关掉鼠标驱动倾斜"后定格画面照样跟鼠标跑** —— 实时与定格各抄了一份倾斜计算，
   定格那份漏了开关（单通道差 252）。现在共用一个 `targetTilt()`。
5. **hash 里的 `'0'` 被读成"开"** —— `clampParam` 对开关写 `v ? 1 : 0`，
   而 hash 解出来是字符串，`'0'` 在 JS 里是**真值**。
6. **canvas 的预乘 alpha 会吃掉掩膜** —— 掩膜放进 alpha 通道后，那两块的其它通道被除以 0 抹平；
   而且画面看着完全正常（GPU 上传不经过 canvas），只有把掩膜过一遍 canvas 才看得见。
7. **卡面扫光有 40% 时间是死的** —— 实测 t=2~5s 整整四秒卡片区平均差正好是 0。
8. **"覆盖卡名颜色"打开等于没打开** —— 默认色和"银字"的银色几乎一样。
9. **圆角半径是拍脑袋写的** —— `CORNER_R = 0.042` 注释里写着"从图上量出来的"，其实没量过。
   把像素摊开一看，这张卡的角是**方**的（`y=26` 那行 `x=26` 就已经是卡片了），
   0.042 × 689 ≈ 29px 等于把卡片自己的四个角各削掉 29px。
10. **卡片外沿检测的阈值写死 0.85** —— 只对"卡片铺满整幅图"有效。造一张"卡片占画面 47% 宽"
    的测试图立刻失效，退化成整张图，**卡名掩膜跟着变成 0%**。
11. **投影被调成了黑边** —— 为了让 `shadowOn` 能被参数扫描判成"接通"，我把投影加到了
    0.70 不透明 / 1.7% 偏移；量出来卡片下沿外侧亮度只有 **9**（远处背景 29），就是一条黑边。
    现在改成"贴着卡片的浅投影"（0.42 / 0.8% / 更宽的模糊）。
12. **检测器的测试里，标准答案自己写错了** —— 素材是"卡片 + 四周 4% 留白"的纹理，
    真值却按"整张图 / 粘贴位置"算，于是每条都差 4%（几十像素），
    看着像检测不准，其实是标准答案错了。

---

## 7. 想改的话，从哪儿下手

| 想做的事 | 改哪儿 |
| --- | --- |
| 加/改一条罕贵度的工艺 | `js/rarities.js` 的 `LIST`（一条 = 一串图层）→ `node tools/gen-doc.mjs` |
| 改某个工艺本身 | `js/shaders.js` 里对应的 `BODY.xxx` |
| 加参数 | `js/config.js` 的 `SPEC`（面板自动生成，不用写 HTML）→ `node tools/gen-lang.mjs --force` 补语言包 |
| **换卡图** | 图片丢进 `images/` → `node tools/embed-card.mjs`（外沿自动识别） |
| **加一种语言** | 复制 `Languages/zh-CN.js` 改名 → 改 `code`/`name`/`texts` → 在 `Languages/languages.js` 加一行 |
| 改卡片快慢/倾斜手感 | 面板「观看」组：倾斜幅度 / 倾斜反向 / 自动摆动 / 时间倍率 |
| 单独看某个工艺 | 面板「调试」组：只看第一层 |
| 核对区域切得对不对 | 面板「调试」组：掩膜图层（红=卡名 绿=卡图 蓝=效果框 灰=卡框 黄=卡图外环） |
| 收起底部说明条 | 说明条右上角的 ▾（状态记在 localStorage 里） |

> ⚠️ **换卡的两个硬编码**：卡框 / 卡图窗 / 效果框是按**游戏王标准版式**写死的比例
> （灵摆卡、连接怪、无效果怪对不上）；卡名抠图写死了"深字浅底"的极性。
> 详见 [`rarity-shaders.md` §10](rarity-shaders.md)。

---

## 8. 出处

- **着色器全部是自己写的**，没有搬运任何游戏的着色器代码。组织方式（"一张精灵纹理 +
  一遍遍往上叠的 draw step"）参考了隔壁 `card-shader/` 对 Balatro 渲染管线的总结。
- **多语言的机制**（语言包分文件、`t()`、localStorage 持久化、缺键回退到 zh-CN）参考了
  隔壁 `Three.js-3D-Earth` 的技术文档；只有加载方式不同（那边 JSON + fetch，这边 .js + script，
  为了 `file://` 双击可用）。
- **卡图是你提供的**；「遊☆戯☆王」相关权利属于 Studio Dice / SHUEISHA / TV TOKYO / KONAMI，
  图上自带 "Replica - Not For Use in Sanctioned Tournaments" 水印。**仅供学习研究，不要商用。**
- `游戏王罕贵度总结（OCG  TCG）.md` 是原始文档，**我一个字都没有改**。
